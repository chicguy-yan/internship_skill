---
name: internship-wiki-builder
description: 帮用户基于「行业认知 / Agent 产品设计 / 前后端研发 / Case 故事库」4 层架构持续累积实习项目全景 wiki，业务领域是 C 端对话/陪伴/创作类 Agent 产品。每当用户分享日报、PRD、技术评审、code review、需求讨论、产品决策、bug 复盘、用户研究、评测结果、或任何实习相关的工作文档/上下文/聊天记录时立即触发；用户提到"沉淀 wiki / 项目复盘 / 实习总结 / 秋招准备 / STAR 整理 / case 故事 / 切入点 / 今天我做了什么"时也立即触发。即使用户没明说要"更新 wiki"，只要内容看起来是实习工作上下文，就主动触发并按本 skill 流程处理。
---

# Internship Wiki Builder

帮用户构建并持续维护一份分层的「实习全景 wiki」，覆盖：

1. **01 行业认知** — C 端对话/陪伴/创作 Agent 行业的赛道/竞品/北极星指标
2. **02 Agent 产品设计** — 产品形态、核心链路、关键决策
3. **03 前后端研发** — 架构、接口、关键模块、工程实践
4. **04 Case 故事库** — 困难闭环型 + 认知反转型故事，用增强版 STAR 写

工作模式：**半自动确认**。skill 给出方案 → 用户确认 → 写入 wiki。

---

## 触发后第一步：识别当前流程

读到用户输入后，先判断走哪条流程：

| 信号 | 走哪条流程 |
|---|---|
| 用户**首次**使用 / 明确说"我先给你完整项目上下文" / wiki 目录还不存在 | **Bootstrap 流程** |
| 用户喂入一份具体工作文档（PRD/讨论纪要/code review/日报片段等），且 wiki 已存在 | **Daily Update 流程** |
| 用户明确说"把这个事做成 case / 这个能扛深挖" | **Case 升级流程** |
| 用户说"看看我的 wiki 现状 / 还缺什么 / 给我提建议" | **Wiki 体检流程** |
| 用户说"整理简历 / 求职素材 / 能力证据 / 秋招 / 扩充我的简历 / 出面试题 / 把我的能力点找出来" | **求职证据地图流程** |

不确定时，用一句话和用户确认走哪条，不要自动推进。

---

## Bootstrap 流程（首次搭建）

目标：把用户提供的项目完整上下文消化成 4 层 wiki 的初版。

### 步骤

1. **询问 wiki 存放位置**：默认推荐 `/Users/fenke/projects/skill/internship_skill/wiki/`，让用户确认或改路径。
2. **读取用户上下文**：可能是一段长文字、多份文档、或一段对话回放。先全文读完再动手。
3. **复制骨架**：把 `assets/wiki_skeleton/` 整个目录复制到目标位置。
4. **填充首批基础条目**：
   - 01 行业认知层：基于上下文里能识别出的赛道/竞品信息，按 `references/industry_C_agent.md` 模板初始化 `业务背景.md` `竞品地图.md` `北极星指标.md`
   - 02 Agent 产品设计层：按 `references/product_design.md` 初始化 `产品形态.md` `核心链路.md` `决策记录.md`（决策记录初始为空表格）
   - 03 前后端研发层：按 `references/tech_RD.md` 初始化 `系统架构.md` `关键模块.md` `工程实践.md`
   - 04 Case 故事库：只放 `README.md`，case 通过 Daily 流程逐步沉淀
5. **更新 INDEX**：在 `00_INDEX.md` 列出所有已创建条目和待补条目（用 `[ ]` 标记）。
6. **输出对话式总结**给用户：
   - 已落地哪些条目
   - 上下文里识别到的**信息缺口**（哪些字段没有素材，需要后续补）
   - 提出 3-5 个**首批 TODO**（建议补的资料/数据/截图）

### 重要约束

- **不要编造**：上下文里没说的字段，留空 + `// TODO: 待补` 标记，不要凭印象瞎填。
- **节制**：首版条目每个不超过 200 字，留扩展空间。
- 写入文件前**先在对话里展示文件清单和每个文件的主要章节**，等用户说"OK 写入"再批量创建。

---

## Daily Update 流程（核心 / 高频使用）

目标：用户每天/每隔几天喂入工作上下文 → skill 出 4 件套 → 确认后增量更新 wiki。

### 步骤

1. **读取现状**：先 `Read` 目标 wiki 的 `00_INDEX.md`，再按需读相关层目录的具体文件（不要一次全读，按判定层精准读）。
2. **分析输入文档**：识别其中涉及的层和条目（参考 `references/wiki_taxonomy.md` 的判定规则）。
3. **生成 4 件套输出**（**严格按 `references/daily_workflow.md` 的 schema**）：
   - **切入点定位**：列出每条信息应落到 `xx_xx/yyy.md` 的哪一节，标记 `[更新]` 或 `[新增]`
   - **今日「我做了什么」STAR 摘要**：S/T/A/R 四段 + mentor 视角推测点评（带 `?` 表明是推测）
   - **可深挖 / 多做的建议**：对照已有 wiki，指出认知盲点 / 可补的数据 / 可写成 case 的高光点
   - **TODO 清单**：结构化分类（待问 mentor / 待跑数据 / 待补素材 / 待复盘 case）
4. **等用户确认**：明确问"是否按上述切入点写入？" 用户可以全部确认、部分确认（指定哪几条）、或要求修订方案。
5. **执行写入**：用 `Edit` 工具增量改已有文件，用 `Write` 工具新建文件（新建时必须基于 `assets/entry_templates/` 对应模板）。
6. **同步更新 INDEX**：新增条目要登记到 `00_INDEX.md`。

### 重要约束

- **绝不"批量重写"已有条目**：永远用 `Edit` 增量加章节/段落，保留用户手写的原内容。
- **保留人话**：用户的口语表达和原始措辞值得保留（更真实），不要全部改成"产品黑话"。
- **可归因优先**：写 STAR 的 R 段时，区分"用户做的事的结果"和"团队整体结果"，不要冒认功劳。

---

## Case 升级流程

目标：把某个具体事件提炼成可扛深挖的 case 故事。

### 步骤

1. 用户指定事件（可能是已经在 wiki 里散落记录、也可能是新讲的故事）。
2. 用 `references/case_storybook.md` 的两种模板之一：
   - **困难闭环型**：卡点（人/资源/认知/机制）→ 拆解 → 推动 → 结果 → 方法论
   - **认知反转型**：初始假设 → 推翻证据 → 调整 → 结果 → 方法论
3. 在 `04_Case故事库/` 下新建 `case_<类型>_<关键词>.md`（基于 `assets/entry_templates/case_entry.md`）。
4. case 的 frontmatter 必须含：
   - `type: 困难闭环 | 认知反转`
   - `tags: [...]`
   - `related: [02_Agent产品设计/xxx.md, ...]`（双链关联到产品/研发层条目）
5. **重要**：每个 case 写完后给用户一段"2-3 分钟口头版本"草稿（用户的「实习框架」要求 case 能口述）。

---

## Wiki 体检流程

用户问"我 wiki 现在咋样 / 还缺什么"时触发。

输出：

1. **覆盖度热力图**：4 层每层条目数 / 字数 / 最近更新时间
2. **盲点清单**：参考行业认知层和产品设计层模板，列出"模板里有但 wiki 还没填的字段"
3. **可升级 case 候选**：扫描 wiki 找含"卡住/翻盘/最终/结果"等词的段落，建议升级
4. **面试可用度评分**：用户的「实习框架」4 步法每步打分（项目全景 / STAR / 故事库 / mentor 视角）
5. **下一步建议**：3 条最高 ROI 的动作

---

## 求职证据地图流程（简历 deliverable）

目标：把已沉淀的 wiki + repo + 飞书文档，转成可直接投递的简历 + 面试 QA。这是「实习 wiki → 求职产物」的下游收口。

> **适用前提（重要，决定本 flow 成不成）**：本 flow 之所以有效，是因为**用户对自己简历要写成什么样、每个分点想表达什么目的，已经有清晰全貌**。skill 的角色是「基于用户已沉淀的上下文，理解他的意图，再扩充 / 改写 / 配证据 / 出面试题」，**不是替用户从零构思简历**。结构、取舍、每个分点的目的都归用户；skill 只做填证据、建双链、套规则、翻黑话、出 grill。**绝不反客为主替用户决定简历该写什么**——用户没给方向时，先问他想表达什么，而不是自己编一版。

### 完整 pipeline

```
① 能力证据地图（扫 repo+文档，按能力维度归类证据，每条带出处+归属）
→ ② 用户给一版简历模板草稿（或直接用证据地图起草）
→ ③ 用证据地图扩充草稿，每个分点填进对应证据并建双链回 wiki 证据层
→ ④ 套两条「扩写规则」成投递终稿
→ ⑤ 用 grill 技能对终稿做面试 QA，附在终稿后面
```

### 步骤

1. **生成能力证据地图**：扫 wiki 各层 + 关联 repo + 飞书文档，按**能力维度**归类证据。
   - 维度**按用户岗位可配置**，不要写死。C 端 Agent 产品经理常用 5 维：产品 sense / 用户洞察 / 市场调研 / AI coding 动手 / 其他常考点（数据驱动·诚实评估·优先级·0→1·跨团队推动·工程严谨）。换岗位就换维度。
   - 每条证据 = `证据点｜出处（双链到 wiki 证据层）｜一句"面试怎么讲"｜归属标签`。
   - 末尾给「跨维度最强的 N 个」+ 统一诚实口径。产出 `05_简历直通车/能力证据地图.md`（或求职层）。
2. **接住用户的简历草稿**：用户常自己先写一版"我需要的简历模板"。读懂他每个分点的**目的**，不要替他改结构。
3. **用证据地图扩充**：把 `xxx`、括号占位、空泛表述填成有出处的具体内容；每个分点尾部加 `> 证据：` 双链；并在被引用的 wiki 文件回链 `> 📍 支撑简历：…`（双向链）。
4. **套两条扩写规则成投递终稿**（见下「简历正文硬规则」）。终稿放 `05_简历直通车/`。
5. **grill 面试 QA**：终稿产出后，**以面试官身份启用 `grill` 技能**对终稿逐条 grill，把所有可能被追问的问题（+ 应答要点 + 诚实边界）列在**终稿正文后面**。grill 的问题用正常标点，不受下面规则 B 约束。

### 简历正文硬规则（生成/扩写简历正文时必须遵守）

- **规则 A · 外行可懂**：简历正文不能假设读者懂业务。出现的每个黑话、缩写、内部术语、专有名词，都要就地翻成大白话，或顺一句话解释它是什么、解决什么问题。判断标准：一个不懂这行的人读完这句能明白你在说什么。宁可多一句解释，也不留没解释的黑话（具体哪些词要翻译，按当前用户的业务领域临场判断，不预设）。
- **规则 B · 标点收敛**：简历正文**只用引号、句号、逗号、顿号四种标点**。不要用括号、斜杠、冒号、分号、破折号、箭头、加号、百分号、方括号等。需要并列用顿号，需要解释用逗号顺下去，需要举例用"比如"带出。

### 归属纪律（本 flow 内置，硬约束）

- 每个证据点/简历分点必须可判归属：**本人独立产出 / 团队产出本人消费 / 工具产物 / 引用数据**；并区分**设计意图 vs 已验证**。
- 简历正文**绝不把团队成果写成个人成果**；面试 QA 里要主动点出诚实边界（哪些是自评、哪些未量化、哪些是 mentor 判断）。

### 半自动约束

- 证据地图、终稿、面试 QA 每一步写文件前，先在对话里展示方案，等用户确认再落盘。

---

## 引用文件

详细的分类规则和模板放在 references/，需要时再读：

- `references/wiki_taxonomy.md` — 4 层分类规则 + 切入点判定
- `references/industry_C_agent.md` — 行业认知层模板
- `references/product_design.md` — Agent 产品设计层模板
- `references/tech_RD.md` — 前后端研发层模板
- `references/case_storybook.md` — Case 故事库模板（增强版 STAR + 两类故事）
- `references/daily_workflow.md` — 每日 4 件套输出 schema 和示例

骨架与单条目模板放在 assets/：

- `assets/wiki_skeleton/` — bootstrap 时整体复制
- `assets/entry_templates/` — 新建条目时按层取对应模板

用户原始的[实习框架](../实习框架)是 case_storybook.md 中 STAR 公式的源头，遇到分歧以原文为准。

---

## 通用约束

- **半自动**：所有写文件动作之前都先在对话里展示方案，等用户确认。
- **保持中文**：用户的 wiki 和所有输出都用中文。
- **不冒充事实**：识别不到的信息留 `// TODO: 待补`。
- **归属四分类**（核心）：写任何"我做了什么"类内容（STAR、case、简历、证据地图）都要可判归属——本人独立产出 / 团队产出本人消费 / 工具产物 / 引用数据，并区分「设计意图」与「已验证」。绝不把团队成果写成个人成果。这是让产出"经得起面试深挖"的根。
- **链路可追溯**：每次更新在条目末尾追加一行 `> 更新源：<日期> <文档名/会议名>`，方便回溯。
- **节制写作**：单个条目首版 ≤200 字，让用户看完愿意改。长篇大论会让用户失去维护意愿。
